# Reproducibility environment for "Underwater Corrosion Detection via YOLO26"
# Captured from the execution environment (server) recorded in the notebook outputs:
#   Ultralytics 8.4.5  |  Python 3.9.13  |  torch 2.8.0+cu128  |  CUDA 12.8 (RTX 4090)
# Pin exact versions for full reproducibility.

# ⚠️ CRITICAL PIN: YOLO26 is NMS-free and its detection decoding CHANGES across
# Ultralytics versions. Verified 2026-07-03: 8.4.5 reproduces every paper number;
# a newer version silently halved the detections (111 -> 53 boxes on the SAM eval set).
ultralytics==8.4.5
torch==2.8.0
torchvision==0.23.0

# Detection ensembling (Weighted Box Fusion)
ensemble-boxes==1.0.9

# Sliced inference (S-TTA branch, notebook 07) — REQUIRED, was missing
sahi>=0.11.14

# Plotting in notebooks 08/09 — REQUIRED, was missing
seaborn>=0.13

# TrashCan pipeline (notebook 08): dataset download + COCO tooling
pycocotools>=2.0.7
gdown>=5.0
requests>=2.31

# SAM 3 (box-prompted refinement) via HuggingFace.
# IMPORTANT: the installed release MUST provide Sam3Model/Sam3Processor — notebook 09
# fails loudly otherwise (no silent SAM 2.x fallback). Verify with:
#   python -c "from transformers import Sam3Model; print('ok')"
# (Pin will be set to the exact version recorded in notebook 09's executed output.)
transformers>=4.58.0
huggingface-hub>=0.30.0
safetensors>=0.4.5

# Scientific stack
numpy>=1.26,<2.0
pandas>=2.0
scikit-learn>=1.3
opencv-python>=4.9
Pillow>=10.0
matplotlib>=3.8
tqdm>=4.66

# Notebook execution
jupyter>=1.0
ipykernel>=6.29
# Required by the tqdm notebook-widget progress bars used inside the notebooks
# (headless nbconvert raises ImportError without it)
ipywidgets>=8.0
